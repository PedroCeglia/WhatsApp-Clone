<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.whatsappclone">

    <uses-feature android:required="true" android:name="android.hardware.camera2"/>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhatsAppClone">
        <activity android:name=".activity.ChatActivity"></activity>
        <activity android:name=".activity.grupo.CadastroGrupoActivity" />
        <activity android:name=".activity.grupo.GrupoActivity" />
        <activity android:name=".activity.ConfiguracoesActivity" />
        <activity android:name=".activity.TelaPrincipalActivity" />
        <activity android:name=".activity.loginEcadastro.LoginActivity" />
        <activity android:name=".activity.loginEcadastro.CadastroActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>